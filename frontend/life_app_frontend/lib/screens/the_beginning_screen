import 'package:flutter/material.dart';

class TheBeginningScreen extends StatelessWidget {
  const TheBeginningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('The Beginning'),
      ),
      body: const Center(
        child: Text('This is where it all begins...'),
      ),
    );
  }
}